{
  "name": "configurator",
  "version": "0.0.1",
  "type": "module",
  "devDependencies": {
    "@sveltejs/adapter-static": "next",
    "@sveltejs/kit": "next",
    "svelte": "^3.51.0",
    "vite": "^3.1.7"
  },
  "scripts": {
    "build": "vite build",
    "dev": "vite dev",
    "preview": "vite preview",
    "check:svelte:watch": "svelte-check --tsconfig ./tsconfig.json --watch",
    "check:svelte": "svelte-check --tsconfig ./tsconfig.json",
    "check:types": "tsc --noEmit",
    "check:e2e": "npm t",
    "check": "run-s check:*",
    "test": "playwright test"
  }
}
